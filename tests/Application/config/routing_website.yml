sulu_media:
    resource: "@SuluMediaBundle/Resources/config/routing_website.yaml"

sulu_website:
    resource: "@SuluWebsiteBundle/Resources/config/routing_website.yaml"

sulu_search:
    resource: "@SuluSearchBundle/config/routing_website.yaml"
    type: portal

# Loaded in both contexts, mirroring the config/routes.yaml import the README documents
mcp:
    resource: .
    type: mcp

sulu_mcp:
    resource: '@SuluMcpBundle/config/routes.yaml'
